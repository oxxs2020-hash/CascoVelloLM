# Static Site Development

- Default to vanilla HTML/CSS/JS (no frameworks) unless user requests otherwise
- Use `npx -y live-server` for local dev server
- Always separate CSS and JS into dedicated files (not inline in HTML)
- Include SEO meta tags, favicon, and accessibility labels by default
- Use Google Fonts (Italiana + Plus Jakarta Sans) for the Casco Vello brand
